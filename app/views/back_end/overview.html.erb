<h1>Übersicht</h1>
<br/>

<table class="table">
  <tr>
    <th>Name</th>
    <th>E-Mail</th>
    <th>Bestellungen</th>
    <th></th>
    <th></th>
  </tr>

  <% @customers.each do |customer| %>
      <% orders = customer.orders %>
      <tr>
        <td><%= "#{customer.salutation} #{customer.name}" %></td>
        <td><%= mail_to customer.email %></td>
        <td>
          <table class="table table-bordered">

            <% orders.each do |order| %>
                <% rows = order.get_short_summary %>
                <tr style="background-color:#FFFFE0;">
                 <td>
                  <table class="table">
                  <% rows.each do |row| %>
                    <tr>
                      <% row.each do |field| %>
                          <td><%= field.to_s %></td>
                      <% end %>
                    </tr>
                <% end %>
                   </table>
                  <td>



                  </td>
                  <td>
                    <p><b>Ereignisse:</b></p>
                    <ul>
                      <li>Bestellung wurde angelegt am <%= order.created_at.in_time_zone("Berlin").to_s(:european_style) %>.</li>
                      <%= if order.order_status_id == "1" then "<li>Bestellung wurde aktiv.</li>".html_safe end %>
                      <%= link_to "Bestellbestätigung verschicken" , url_for(:controller => "orders", :action => "send_confirmation_mail", :id => order.id) %>

                    </ul>

                  </td>
                </tr>
            <% end %>

          </table>
        </td>

        <td>Button</td>
      </tr>
  <% end %>
</table>

<br />

